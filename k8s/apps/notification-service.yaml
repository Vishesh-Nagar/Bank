apiVersion: apps/v1
kind: Deployment
metadata:
  name: bank-notification-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bank-notification-service
  template:
    metadata:
      labels:
        app: bank-notification-service
    spec:
      containers:
        - name: notification-service
          image: my-registry/bank-notification-service:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: bank-config
            - secretRef:
                name: bank-secret
          ports:
            - containerPort: 8084
---
apiVersion: v1
kind: Service
metadata:
  name: bank-notification-service
spec:
  ports:
    - port: 8084
  selector:
    app: bank-notification-service
